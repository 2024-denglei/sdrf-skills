# SDRF skills — pip dependencies
# For Thermo .raw files, thermorawfileparser is not on PyPI; use conda:
#   conda install -c bioconda thermorawfileparser
#
# Install:
#   pip install -r requirements.txt

# SDRF validation and raw file analysis
sdrf-pipelines[ontology]
techsdrf

# Tools dependencies (tools/ package)
requests>=2.28
pydantic>=2.0

# Optional: AI cross-validation backends (only needed for `python -m tools crossval`)
# anthropic         # Claude API
# openai            # OpenAI API
# google-generativeai  # Gemini API
